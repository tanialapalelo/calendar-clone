# Web (Next.js)

Next.js (App Router) frontend for the Calendar Clone monorepo.

- Local setup and environment variables live in the **root** [`README.md`](../../README.md).
- Run locally: `pnpm -C apps/web dev`
- App: http://localhost:3000
