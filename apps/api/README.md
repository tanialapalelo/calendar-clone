# API (NestJS)

NestJS + Prisma API for the Calendar Clone monorepo.

- Local setup, environment variables, and Google OAuth configuration live in the **root** [`README.md`](../../README.md).
- Run locally: `pnpm -C apps/api dev`
- Health check: `GET http://localhost:3001/v1/health`
